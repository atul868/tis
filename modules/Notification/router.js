const hasRole = require('../../middlewares/hasRole');
const middleware = require('../../middlewares/validation');
const auth = require('../../utils/auth');
const authCheck = auth.jwt;

const { } = require('./controller')

const {  } = require('./vailidator')

module.exports = (app) => {
};